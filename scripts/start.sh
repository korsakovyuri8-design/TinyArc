#!/bin/sh
# Боевой запуск: проверка окружения, миграции, сервер.
#
# Отдельный файл, а не строчка в конфиге хостинга: длинную команду с вложенными
# кавычками каждый хостинг разбирает по-своему. В файле кавычки принадлежат
# только shell.
set -e

# Первым делом и до базы: недостающий секрет должен остановить выкладку, а не
# превратиться в сайт, который поднялся и отдаёт 500 на каждой странице.
npx tsx scripts/preflight.ts

npx prisma migrate deploy

exec npx next start
