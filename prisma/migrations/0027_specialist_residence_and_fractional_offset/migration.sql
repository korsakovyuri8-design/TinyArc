-- Пул становится мировым.
--
-- Страна проживания отдельно от стран проектов: архитектор из Бразилии может
-- вести концепцию черногорской виллы, но Бразилии среди стран проектов нет,
-- и до сих пор ему нечего было отметить в анкете.
--
-- Смещение часового пояса становится дробным. Целых часов хватало Европе;
-- Индия живёт по +5:30, Непал по +5:45, Иран по +3:30, и движок считал бы им
-- пересечение рабочего дня с ошибкой в полчаса.
ALTER TABLE "Specialist" ADD COLUMN "residenceCountry" TEXT NOT NULL DEFAULT '';
ALTER TABLE "Specialist" ALTER COLUMN "utcOffset" TYPE DOUBLE PRECISION;
