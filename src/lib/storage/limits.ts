/**
 * Потолок на файл.
 *
 * Отдельным модулем без единого импорта: значение нужно и серверу, и форме в
 * браузере, а `index.ts` тянет за собой работу с диском. Клиентский компонент,
 * потянувший `node:fs`, роняет сборку целиком — и роняет её в файле, который
 * про файлы вообще ничего не знает.
 *
 * Больше этого — уже не чертёж, а чей-то архив.
 */
export const MAX_FILE_BYTES = 50 * 1024 * 1024
